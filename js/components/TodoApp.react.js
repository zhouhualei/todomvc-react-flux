

var Header = require('./Footer.react');
var React = require('react');

var TodoApp = React.createClass({
    render: function() {
        return (
            <div>
                <Header />
            </div>
        );
    }
});